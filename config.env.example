# SnapUp 环境变量配置示例
# 复制此文件为 .env 并根据需要修改

# ======================================
# 基础配置
# ======================================

# HTTP 服务端口
PORT=8080

# 截图输出目录
OUTPUT_DIR=./screenshots

# 运行模式: http 或 mcp
RUN_MODE=http

# ======================================
# Chrome 配置
# ======================================

# Chrome WebSocket URL
# 留空: 使用本地 Chrome
# Docker 部署: ws://chrome:9222
# 远程 Chrome: ws://your-server-ip:9222
CHROME_WS_URL=

# 使用公网 Chrome 的示例配置
# CHROME_WS_URL=ws://your-server.com:9222
# CHROME_WS_URL=wss://chrome.your-domain.com

# ======================================
# 内网穿透配置 (frp)
# ======================================

# frp 服务端地址
FRP_SERVER_ADDR=your-frp-server.com
FRP_SERVER_PORT=7000
FRP_AUTH_TOKEN=your-secret-token

# 远程暴露端口
FRP_REMOTE_PORT=9222

# ======================================
# Cloudflare Tunnel 配置
# ======================================

# Cloudflare Tunnel Token
# 通过 cloudflared tunnel token <tunnel-name> 获取
CLOUDFLARE_TUNNEL_TOKEN=

# ======================================
# 安全配置
# ======================================

# HTTP 基础认证 (可选)
# 如果启用，访问时需要提供用户名和密码
# HTTP_AUTH_ENABLED=true
# HTTP_AUTH_USERNAME=admin
# HTTP_AUTH_PASSWORD=your-secure-password

# 允许的 IP 白名单 (可选)
# 逗号分隔，例如: 1.2.3.4,5.6.7.0/24
# ALLOWED_IPS=

# ======================================
# 日志配置
# ======================================

# 日志级别: debug, info, warn, error
LOG_LEVEL=info

# 日志输出: stdout, file, both
LOG_OUTPUT=stdout

# 日志文件路径 (当 LOG_OUTPUT=file 或 both 时)
LOG_FILE=./logs/snapup.log

# ======================================
# 性能配置
# ======================================

# 最大并发截图数
MAX_CONCURRENT_SCREENSHOTS=5

# 截图超时时间 (秒)
SCREENSHOT_TIMEOUT=30

# Chrome 启动超时时间 (秒)
CHROME_STARTUP_TIMEOUT=60

# ======================================
# 其他配置
# ======================================

# 是否保留截图历史
KEEP_SCREENSHOT_HISTORY=true

# 截图历史保留天数
SCREENSHOT_HISTORY_DAYS=7

# 自动清理旧截图
AUTO_CLEANUP_ENABLED=true

